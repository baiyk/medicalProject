// Generated by CoffeeScript 1.10.0
(function() {
  $(function() {
    var GLOBAL_COOKIE, getCookieStr, getCookieVal, setCookie;
    getCookieVal = function(offset) {
      var endstr;
      endstr = document.cookie.indexOf(";", offset);
      if (endstr === -1) {
        endstr = document.cookie.length;
      }
      return decodeURIComponent(decodeURIComponent(document.cookie.substring(offset, endstr)));
    };
    getCookieStr = function(name, path) {
      var alen, arg, clen, i, j;
      arg = name + "=";
      alen = arg.length;
      clen = document.cookie.length;
      i = 0;
      j = 0;
      while (i < clen) {
        j = i + alen;
        if (document.cookie.substring(i, j) === arg) {
          return getCookieVal(j);
        }
        i = document.cookie.indexOf(" ", i) + 1;
        if (i === 0) {
          break;
        }
      }
      return null;
    };
    setCookie = function(cookiename, cookievalue, hours, path) {
      var date;
      if (hours == null) {
        hours = 24;
      }
      if (path == null) {
        path = '/';
      }
      date = new Date();
      date.setTime(date.getTime() + Number(hours) * 3600 * 1000);
      return document.cookie = cookiename + "=" + cookievalue + "; path=" + path + ";expires = " + date.toGMTString();
    };
    GLOBAL_COOKIE = {};
    return $.cookie = {
      getCookie: function(name) {
        if (GLOBAL_COOKIE[name]) {
          return GLOBAL_COOKIE[name];
        } else {
          return getCookieStr(name);
        }
      },
      setCookie: function(key, value, hour, path) {
        return setCookie(key, value, hour, path);
      }
    };
  });

}).call(this);
